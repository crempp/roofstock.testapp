apiVersion: v1
kind: Service
metadata:
  name: testapp-website
  namespace: {{ default "staging" .Release.Namespace }}
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: http
      name: http
  selector:
    app: testapp-website
  type: ClusterIP